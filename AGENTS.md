# Personal-Web 开发与交接规范

本文件是所有参与本项目的 AI agent 的共同入口。开始工作前阅读本文件、README.md，以及已存在的 ARCHITECTURE.md、PROGRESS.md、docs/design-map.md。先检查工作区实际状态，再继续已有工作。用户后续明确指示优先于本文；需求发生变化时同步更新相关文档。

## 1. 已确认的目标与范围

- 为 Shuoyue Wu 开发静态个人作品网站，正式域名为 https://shuoyuewu.com。
- 技术框架必须为 Vite + React；采用 TypeScript、npm 与提交到仓库的 lockfile。
- 首版仅英文，包括 Work 首页、About，以及 Noe、CUE、JingDong、COVID19、Eureka、CAPE、ErosTrilogy 七个项目详情。
- 本阶段以桌面设计还原与不同桌面宽度下的弹性布局为验收重点。小屏暂时基本可读、可导航；正式小屏设计尚未完成，不得将临时适配标记为设计验收完成。
- 后续主要偶尔增加作品。用户会在 Figma 新建 frame 或修改已有 frame，再让 AI 更新代码。无需建立 CMS、后台或可视化内容编辑器。
- 动画和交互以 Figma annotation 为准，没有额外参考；参数应集中配置，便于用户手调。
- 与用户沟通和交接文档默认中文；代码命名使用英文；网站面向访客的内容使用英文。

## 2. Figma 设计来源与读取规则

文件：https://www.figma.com/design/FueBrub7aXMt8Jl0pnoF6Q/个人网站设计2?m=dev

File key：`FueBrub7aXMt8Jl0pnoF6Q`。

| 内容 | Node ID |
| --- | --- |
| WorkingArea | 2:119 |
| Assests（Figma 中的原名） | 46:5018 |
| JG / Components & States | 12:8 |
| Work / Desktop | 35:3836 |
| About / Desktop | 35:4033 |
| Noe | 35:4055 |
| CUE | 71:8057 |
| JingDong | 71:8172 |
| COVID19 | 71:8287 |
| Eureka | 71:8402 |
| CAPE | 71:8517 |
| ErosTrilogy | 80:8779 |

节点链接格式：`https://www.figma.com/design/FueBrub7aXMt8Jl0pnoF6Q?node-id=35-3836`。后续 frame 可能新增、移动或替换，应核对实际节点，不以旧 ID 推断新设计。

设计依据优先级：用户最新明确指示 > 当前画板的 annotation > 当前画板视觉与组件状态 > 通用组件说明 > 合理且有记录的实现推断。忽略评论区；不要要求用户把 annotation 再口述一遍。若 annotation 与画面有明显矛盾，记录冲突并询问影响实现的关键点。

- 实现前获取目标节点的 design context、annotation 和截图；不能只根据节点名或缩略图开发。
- 有对应 Figma skill 时遵循其读取流程。工具生成代码只是参考，必须按本项目架构和样式体系改写。
- 大节点返回元数据或被截断时，分区继续读取，尤其 CAPE；元数据成功不等于设计已完整读取。
- 已确认能读取设计标注。规划阶段工具不支持 `devStatus`，不能声称已独立验证 Ready for Dev；用户已确认上表的首版页面范围，可直接依据该范围开发，无需因此阻塞。
- `Assests` 中的 `Archived` 是历史设计，不得混入当前页面。素材可在核对后复用，历史文案和外链不能自动继承。
- 已发现组件中存在 `jackygrob.com` 等旧模板链接；核对所有 href、人物名称、项目名称和占位内容。目标不明时列为待确认，不得发布无关模板链接，也不得自行编造。
- 保留用户设计与文案；不要自行重写英文介绍、增删项目或改变视觉风格。疑似错误单独记录。

## 3. 目录与组件约定

新站位于仓库根目录。建议基础结构如下；只创建实际需要的文件和目录，不提前搭建空壳体系。

```text
src/
  app/                 # 应用入口、路由与全局装配
  pages/               # Work、About、项目详情页面组合
  components/
    layout/            # 导航、页脚、页面容器
    ui/                # 按钮、标签、角标等通用元素
    projects/          # 项目卡片、项目信息、详情内容模块
  features/
    hero/              # 点阵、Three.js Logo 场景及其局部逻辑
  content/             # 项目数据、文本、媒体引用和区块顺序
  config/              # 网站元信息、路由约定、可手调动画参数
  hooks/               # 真正跨组件复用的行为
  styles/              # 全局 reset、设计 tokens、排版基础
  assets/              # 由构建工具处理的素材
public/                # 需要稳定路径的模型、静态文件等
docs/
  design-map.md        # Figma 到代码的映射、标注要求、设计差异
  verification/       # 必要的截图、验收记录
Archive/               # 现有旧站，独立保留
```

- React 组件文件使用 PascalCase；hooks 使用 useXxx；数据和配置文件统一采用项目确定的一种命名方式。样式与组件就近存放，推荐 CSS Modules + 全局 CSS variables。
- 不因 Figma 输出 Tailwind 就自动安装 Tailwind；不引入大型 UI 库来替代定制设计。
- 共用组件包括 Navigation、Footer、ProjectCard、InfoCell、ProjectLabel、DetailRow、BorderCorners、项目介绍和媒体模块。
- 页面负责组合；组件负责呈现和局部行为；内容文件负责文本、素材和项目区块顺序。七个项目详情可复用内容模块，但不得为了统一模板抹平各页差异。
- 避免巨大单文件，也避免把所有简单标签机械拆分。仅在存在明确复用或职责边界时抽象。
- 项目使用稳定 slug，集中维护卡片到详情页的映射。新增作品复用模块；确实需要新编排时扩展模块并记录，不建立过度通用的页面搭建系统。
- 依赖选择以必要性为准；检查旧站实现是否可参考，但新站不直接依赖 Archive 的源码或构建产物。

## 4. 弹性布局与设计还原

- **极度优先 Flexbox，尤其桌面端。** 用 flex-direction、flex-wrap、flex-basis、grow/shrink 和 min/max-width 表达布局。仅当 Flex 明显无法合理表达时使用 Grid，并在设计映射中说明原因。
- 1512px 是当前主要桌面画板的参考宽度，不是整站固定宽度。不得把整个网页当成固定画布缩放，不得将 Figma x/y 全面翻译成绝对定位。
- 保留设计的列比例、边距、行间距、图片比例和文本层级。固定值用于有明确意图的细节，主体容器应随可用宽度调整。
- 文字内容驱动高度，合理设置 min-width: 0、换行和 overflow，避免用固定高度裁掉正文。
- 四角装饰相对所属容器定位并始终贴合边界。装饰、叠层等可有意使用绝对定位，不应强行纳入文档流。
- 首页指定介绍区域和 About 的相关文本区域遵循 annotation 的 75% 宽度意图：按对应容器可用宽度计算，不机械使用 75vw。
- 缺少 Auto Layout / Fill Container 时补足网页弹性行为，并在 docs/design-map.md 记录实现解释。小幅技术修正可自主完成，重大视觉或内容变化向用户确认。
- 小屏临时方案：内容按合理阅读顺序堆叠、媒体不越界、导航可用、文字可读；无 hover 的设备仍能访问项目信息和链接。不强制用户切到桌面，不隐藏主要内容。

## 5. 字体、颜色和资源

全站主字体为 Didact Gothic，正常体，weight 400。优先在 HTML head 使用用户提供的 link 方式，避免同时再用 @import 重复加载：

```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Didact+Gothic&display=swap" rel="stylesheet">
```

```css
font-family: "Didact Gothic", sans-serif;
font-weight: 400;
font-style: normal;
```

- 字号、行高、颜色、间距和边框从当前 Figma 提取到统一 tokens。不要因为存在多个相似值就盲目合并有意的差异。
- 避免无意使用浏览器合成粗体。截图验收等待字体加载完成，同时检查字体失败时仍可阅读。
- 使用真实 Figma 导出素材；不得凭记忆重画图标、用近似素材替换或遗留无说明的占位图。
- Figma 临时资源链接会过期。正式代码必须使用下载保存的资源或明确的长期媒体来源，并记录 Figma 来源。素材按项目组织、名称可读。
- 图片保持设计裁切意图，适当压缩，非首屏图片按需加载，并设置尺寸/比例以减少布局跳动。不要把应当是正文的内容整页导出成图片。
- 根目录已有用户提供的 `LOGO3D.obj`，规划时为未跟踪文件；不得覆盖、删除或视为垃圾文件。开发时检查模型并复制到合适的运行时资源目录，记录去向；如转换格式须保留原始文件。

## 6. 已读取的交互要求

实现前仍须对照最新 annotation，以下是规划阶段的需求摘要。

- Hero 点阵：鼠标周围约 75px 半径内点稍微放大、变亮，影响随距离衰减。
- Hero Logo：使用 Three.js 实现持续旋转的 3D 模型、光滑高反光 PBR 金属材质、三四个基础面光源以及鼠标控制的额外强光源。可通过 React Three Fiber 集成；具体依赖与场景方案记录在架构文档。
- 首页介绍区域滚出视口后出现悬浮导航，顶部间距 24px；Logo 从导航边框内左侧滑入，平滑推动 Work、About 到目标位置。其他页面共用导航依照当前画板实现。
- 导航按钮 default/hover 前景与背景反转。组件说明给出 350ms，可作为初值。
- 项目卡片 default/hover 平滑切换：原图上划渐隐、目标封面渐显；标签文字上划移出、同名文字从下方进入。反向状态同样连续。不要只写 opacity 而遗漏位移。
- 卡片通用说明含 144px 元信息区域与 350ms Smart Animate；各实例实际状态以当前画板及 annotation 为准。
- Noe 的视频暂用 YouTube `3k7AbJDKz3k`，标记为待用户后续替换。
- COVID19 指定的三张图片链接到 https://mr-speakless.github.io/ITP-dataVisualize-dashboard/。
- Eureka 视频为 YouTube `_Y6XSFS4PVM`；ErosTrilogy 中已标注 `gqkByELFMBA` 和 `3lSUb1UaHg4`。按对应节点放置，核对标注中的转义字符，不将 Markdown 转义误写进 URL。
- 视频使用可维护的媒体配置，响应式保持比例，提供 title 和播放控件；默认不擅自启用有声自动播放。

将旋转速度/方向、材质参数、灯光强度/位置、鼠标映射、点阵范围/增幅、动画时长/缓动与导航间距集中在如 `src/config/motion.ts` 的有类型配置中，并解释每项单位与影响。未指定参数是可调整的实现默认值，不宣称为设计要求。

动画应在组件卸载时释放监听、渲染循环和 GPU 资源；页面隐藏或 Hero 离屏时避免无用渲染。支持 prefers-reduced-motion，并在 WebGL 不可用时提供合理静态回退，不能让网站主体因此无法使用。

## 7. 路由、部署和旧站保护

- 页面跳转无需用户重新逐项说明：Work 导航到首页 `/`，About 到 `/about`，项目卡片到对应项目详情；明确的媒体、联系与外部链接按当前 annotation 和画板目标实现。AI 自主确定一致的项目 slug 并记录映射。只在发现冲突、旧模板链接或无法确定的目标时询问具体缺口，不重复要求完整跳转说明。
- 当前仓库已通过 `.github/workflows/deploy.yml` 在 push 到 main 后发布 GitHub Pages。新站在根路径 `/`，Vite base 为 `/`；旧站保持 `/old/`。
- 域名配置由用户完成，不擅自重建 DNS 或更换托管平台。域名用于网站元数据等；不把完整域名误当成 Vite base 的必填值。
- 现有 workflow 只构建 Archive 并发布 site-root 占位页。开发 agent 必须接入根目录新站构建，再将旧站产物组装到 `/old/`，维护各自依赖缓存和锁文件。
- 保留 `/old/CN`、`/old/ENG` 以及旧站既有深层路径与资源。新站全局样式、路由、404 和发布组装不能覆盖旧站。
- 为新站干净 URL 设计静态路径输出/预渲染方案，确保从地址栏直接访问与刷新项目详情可用。不要仅验证站内点击，也不要未经说明改成 hash URL。
- 配置正确的页面标题、description、canonical、分享预览和 favicon；公共路由清单可用于 sitemap。具体未知的分享图素材列为待补充，不自行编造。
- 优先在分支上开发与本地验证，遵循用户已有 Git 指示。修改文件不等于已发布；在 PROGRESS.md 区分本地、已提交、已推送、部署成功四种状态。不要仅因规划文档提到上线就自行推送 main。

## 8. 验收标准

- **职责分工：AI 自主完成快速技术验证，大部分视觉验证由用户负责。** AI 仍须按 Figma 实现，但不默认进行耗时的逐页截图比对、多轮像素微调或完整跨尺寸视觉验收。交付可用的本地预览、页面入口和简短视觉待审清单，等待用户反馈后针对性修改。用户视觉审阅不阻塞其余已明确的开发工作。
- 项目可从 lockfile 复现安装，提供并通过 build、类型检查与 lint；具体命令写入 README。
- 页面完整、素材正确、路由与外链可用，无无关模板链接、控制台错误、关键资源 404 或无说明占位内容。
- AI 做轻量浏览器冒烟检查，确认页面能打开、无明显溢出或内容遮挡、小屏基本可读。1512px 可作为桌面参考，390px 可作为小屏检查点，不代表指定断点。更多尺寸与精细视觉比对默认留给用户；只有定位具体问题或用户要求时才扩展检查。
- AI 快速检查链接点击、键盘焦点、导航触发、媒体/模型加载等关键功能；减少动画模式和 WebGL 回退应有合理行为。用户主要检查字体排版、间距、图片裁切、动效流畅度、材质与灯光观感。关键动作不能只靠鼠标 hover。
- 使用语义化链接和按钮，合理的标题层级，图片替代文本；装饰不污染辅助技术阅读。
- 检查 `/`、`/about`、每个最终确定的项目路径的直接访问与刷新；检查旧站入口与代表性深层路径。记录本地模拟与线上验证的区别。
- 页面验收区分：已实现、已技术验证、待用户视觉验收、用户已确认。只有用户实际确认后才标记视觉验收通过；没有真正运行的检查写“未验证”，不得将构建成功等同于视觉一致。
- 针对关键行为和回归风险写必要测试，不为简单静态展示强加庞大测试体系。快速检查通过后继续推进，不为重复检查或自行追求像素一致消耗大量时间。

## 9. 文档与跨 agent 交接

开发开始时建立或更新以下文件；本规划交付只创建 AGENTS.md，不意味着这些文件已经齐备。

| 文件 | 必须包含 |
| --- | --- |
| README.md | 项目目标、运行环境、安装/启动/检查/构建/预览方式、更新作品流程、字体与资源说明、发布和旧站入口 |
| ARCHITECTURE.md | 实际文件结构及职责、组件边界、项目内容组织、路由与静态输出、动效配置位置、部署组装、关键技术决定及原因 |
| PROGRESS.md | 当前阶段、页面/组件状态、已完成内容、正在修改内容、具体文件、未完成项、验证证据、阻塞和下一步 |
| docs/design-map.md | 每个画板到页面/组件/路由的映射、annotation 清单及实现状态、素材来源、响应式补足、参数默认值、待确认差异 |

PROGRESS.md 顶部维护一个简洁的最新快照，后附简短交接记录，避免日志无限膨胀。每个可验收阶段完成时和停止工作前更新，不能只在额度耗尽前才尝试记录。

交接快照至少包括：

```text
更新时间（含时区）：
当前分支 / 已核实的提交：
当前目标与阶段：
已完成且已验证：
已实现但待验证：
进行中（具体文件和剩余步骤）：
未提交改动与用户原有文件：
已执行检查（命令、结果、检查宽度、证据路径）：
已知问题 / 待用户确认：
本地与发布状态：
下一位 agent 应先做的 1–3 件事：
```

- 页面清单至少包含 Work、About、七个详情页及共用组件；用明确状态描述，不以没有证据的完成百分比代替。
- 下一位 agent 先验证进度文档与工作区是否一致，再接续未完成项；不得因为换平台就重搭项目或丢弃之前决定。
- 所有关键决定和待办落在仓库文档中，不依赖某个平台的聊天记忆、本地绝对路径或专属工具。
- Figma 更新流程：确定变更 frame → 重新读取对应 context/annotation → 核对共用组件影响 → 最小范围修改 → 验证受影响页面 → 更新映射与进度。
- Figma 暂不可用时，可以继续已记录且不依赖新设计的工作；缺失视觉信息要明确标记，不凭猜测完成设计验收。

## 10. 规划交付时的真实基线

截至 2026-09-19，本轮只完成需求确认、设计读取和本规范。新站尚未初始化、未实现页面、未进行构建或浏览器验收。现有根 README 和发布流程描述的是旧站 + 新站占位页。不要将本文件中的目标目录、组件、参数配置与验收项误记为已完成。

建议实施顺序：文档与项目骨架 → tokens/字体/共用布局 → Work 和 About → 七个项目详情 → 完整动效与可调参数 → 快速技术检查及静态路由 → 发布流程集成与交接，提供预览供用户视觉验收。可提前验证 3D 模型加载等高风险部分；每一阶段留下可继续的实际成果。
